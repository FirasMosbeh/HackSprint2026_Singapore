# Demo mode: run the UI against the built-in mock audition engine.
# Set to false once agent/ + testing/ expose the real API.
VITE_DEMO_MODE=true

# Base URL of the real backend (used when VITE_DEMO_MODE=false).
VITE_API_URL=http://localhost:8000
